---
// Layout import — provides basic page elements: <head>, <nav>, <footer> etc.
import BaseLayout from "../layouts/BaseLayout.astro";

// Work page component
import WorkComponent from "../components/WorkPage.astro";

// Work images metadata
import WorkImages from "../data/work-images.ts";

const materials = ["cloth;", "pine beam;", "DC motors."];

const image1 = {
  src: WorkImages.movingcloth.gallery[0],
  alt: "Outside shot of the piece.",
};

const image2 = {
  src: WorkImages.movingcloth.gallery[1],
  alt: "Close up shot.",
};

const image3 = {
  src: WorkImages.movingcloth.gallery[2],
  alt: "Close up shot from behind.",
};
---

<BaseLayout>
  <WorkComponent
    name="moving cloth"
    year="2020"
    materials={materials}
    description="A machine that displays free will and interacts within its restricted space."
    location="Limina, Porto."
    imageLink1={image1}
    imageLink2={image2}
    imageLink3={image3}
    videoLink="https://player.vimeo.com/video/485596818?h=26d7b4b656&title=0&byline=0&portrait=0"
  />
</BaseLayout>
