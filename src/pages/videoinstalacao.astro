---
// Layout import — provides basic page elements: <head>, <nav>, <footer> etc.
import BaseLayout from "../layouts/BaseLayout.astro";

// Work page component
import WorkComponentTop from "../components/WorkTopPage.astro";
import WorkComponentBot from "../components/WorkBotPage.astro";

// Work images metadata
import WorkImages from "../data/work-images.ts";

const materialsTop = ["metal rod."];

const image1Top = {
  src: WorkImages.videoinstalacao.gallery[0],
  alt: "Side view of the piece in the space.",
};

const image2Top = {
  src: WorkImages.videoinstalacao.gallery[1],
  alt: "Overview of the piece.",
};

const materialsBot = ["Cloth;", "DC motors;", "Beams."];

const image1Bot = {
  src: WorkImages.presencamutavel.gallery[0],
  alt: "Detail of the piece.",
};

const image2Bot = {
  src: WorkImages.presencamutavel.gallery[1],
  alt: "Overview of the piece.",
};
---

<BaseLayout>
  <WorkComponentTop
    name='"("'
    year="2020"
    materials={materialsTop}
    description="Video installation, interventions over images from gallery Dentro."
    location="Galeria Dentro, Porto."
    imageLink1={image1Top}
    imageLink2={image2Top}
    videoLink="https://player.vimeo.com/video/485596621?h=6eb82d152e&title=0&byline=0&portrait=0"
  />
  <WorkComponentBot
    name="presença mutável"
    year="2020"
    materials={materialsBot}
    description="Video installation, interventions over images from gallery Dentro."
    location="Galeria Dentro, Porto."
    imageLink1={image1Bot}
    imageLink2={image2Bot}
    videoLink="https://player.vimeo.com/video/485595685?h=15a99c0b2e&title=0&byline=0&portrait=0"
  />
</BaseLayout>
